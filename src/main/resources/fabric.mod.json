{
  "schemaVersion": 1,
  "id": "serverredirect",
  "version": "${version}",
  "name": "ServerRedirect",
  "description": "This mod allows servers to redirect clients to other servers.",
  "authors": [],
  "contact": {},
  "license": "MIT",
  "icon": "assets/serverredirect/icon.png",
  "environment": "*",
  "entrypoints": {
    "main": [
      "net.kaikk.mc.serverredirect.fabric.ServerRedirect"
    ]
  },
  "depends": {
    "fabricloader": ">=${loader_version}",
    "fabric": "*",
    "minecraft": "${minecraft_version}"
  }
}
